# 更新记录

## 4.15.0 — 2026-09-12

版本标签为 `v4.15.0`，镜像为 `sexyfeifan/coralbay-rules:4.15.0`。发布记录见 [GitHub Releases](https://github.com/sexyfeifan/Coralbay-Rules/releases)，部署与升级分别验收。

### 妙妙屋X 的 YYDS 模板与规则集

- 新增独立 `/miaomiaowu` 子页面，位于“客户端配置 → 妙妙屋X 模板”；提供完整模板与单项规则集两个导入入口，按需加载，不修改已有订阅、PPanel 模板或 MihomoPro 覆写设置。
- 基于 666OS / YYDS Pro_cn 生成妙妙屋X Clash V3 YAML 模板，保留 33 个 MRS 规则集、17 个业务组以及原序的 28 条 `RULE-SET` 和最后一条 `MATCH`。节点层适配为全球手动、全球自动与故障转移三个组，共 20 组；不复刻原版地区优先策略，节点由妙妙屋X 注入。
- 为 33 个原始规则集提供按项生成的 `payload` YAML、正文预览、下载和 `rule-providers` 引用片段。生成结果使用 `format: yaml`、`behavior: classical`，可在妙妙屋X“规则集管理”选择“手动维护”，粘贴正文或从文件导入。
- 大规则集在屏幕上最多预览前 200 条，明确显示预览条数与完整条数；复制和下载保持完整正文，避免十万行规则拖慢页面。
- 完整模板与规则集各自选择本机镜像或同版本上游源。“本机”指 CoralBay 服务器；规则集选项控制生成时读取哪一份原件，生成的 YAML 仍由 CoralBay 提供。来源或条目切换立即清空旧结果，失败不会保留旧有效下载链接。
- 模板和规则集均按固定版本交付，规则更新后需重新获取或生成并导入。新增模板目录 `data/rule-templates/miaomiaowu/` 与规则集目录 `data/miaomiaowu/rulesets/`；备份仍须覆盖整个 `data/`，包括依赖的原件和解码缓存。

详细操作与适配边界见[妙妙屋X 使用指南](docs/miaomiaowux.md)，验证结果和备份升级方式见 [4.15.0 发布说明](RELEASE-4.15.0.md)。发布版本不会自动替换用户在妙妙屋X 中已经绑定的模板或已保存的规则集。

## 4.14.0 — 2026-09-08

本节描述代码变更；版本标签为 `v4.14.0`，镜像为 `sexyfeifan/coralbay-rules:4.14.0`。实际发布状态见 [GitHub Releases](https://github.com/sexyfeifan/Coralbay-Rules/releases)，升级方法见[搭建与升级指南](docs/deployment.md)。发布与线上升级分别验收。

### 来源选择与交付

- MihomoPro 覆写、PPanel 模板、普通订阅转换、自定义分流统一使用“分流规则来源：本机镜像／上游源”。选项属于当前产物，不修改节点订阅地址，不跨规则库替换同名规则。
- MihomoPro 覆写与完整配置成对提供来源、版本固定的 URL，所有复制、下载与预览指向所选变体。覆写使用专属客户端文件名，按填写的主、备用订阅建立完整节点提供者。YYDS 配置摘要参与发布身份，配置单独更新也能生成新版本。
- 普通转换的内置 MihomoPro 预设为 Clash/Mihomo、Stash 完整配置提供生成时双源：校验同一 666OS 版本的 33 份 MRS 原件，按原有规则顺序完整展开实际引用的 28 个集合及兜底。其余资源为备用，不擅自加入路由。结果内嵌规则，并返回实际来源、版本、有效条目、未覆盖项和逐资源记录。
- PPanel 的 Clash（Mihomo 内核）、Mihomo、OpenClash、Stash MRS 改造模板支持等价双源。Perfect Panel 原始版／改造版独立选择；其他格式的本机派生文件没有等价上游原件时，显示原因并保持兼容方式。
- 旧普通链接缺省 `rule_source` 时保留原签名与规则链路，旧自定义方案缺省来源字段时保持内嵌，不自动迁移。普通转换改来源须生成新链接；自定义方案主动保存后保留原固定订阅地址。

### 资源维护与可靠性

- 两个资源列表始终保留本机镜像、上游原文件和详情入口；缺失、损坏或只有兼容缓存时显示原因与同步／修复操作。两侧原始文件均验证成功后才能比较摘要，旧缓存摘要不能证明原件存在。
- MetaCubeX 将上游检查、同步最新版本、修复已知固定版本分开。版本检查接口不可用时仍可尝试修复已知提交；历史修复不切换当前版本。
- MetaCubeX 的维护折叠区按需加载历史版本分页元信息，显示磁盘总量、剩余空间、规则占用与告警，并可修复指定历史版本。历史列表不声称所有文件已重新验证；已交付版本继续留存，不自动清理。
- 普通转换后端响应上限 16 MiB，远程 INI 缓存上限 2 MiB；超限拒绝，不交付截断后可解析的片段。无效配置保留最后有效缓存，并避免并发缓存写入互相覆盖。
- 原始 MRS 解码使用镜像内固定内核，解码缓存校验原件与文本摘要。缺少原件、摘要不符、无法保持规则语义或解码超限时明确失败，不切换来源兜底。
- Stash 节点处理补充 Hysteria2 原生认证／速率字段适配和 TUIC 版本校验，冲突或未支持参数继续报错。

### 支持与验证边界

88 份第三方 INI 的全部嵌套规则未完成双源适配；“配置文件来源”仍只控制 INI 文件读取位置。普通转换其他客户端目标暂不开放新规则来源，纯节点输出不适用。旧 geo 派生文本／JSON 和旧 INI 不会被批量改写，也不因新 MRS 解码能力就被称为等价原件。

完整历史回退界面、离线资源包导入导出、全局切换旧订阅和自动删除历史引用不在本版范围。固定资源、历史和检查缓存会占用磁盘，需保留整个 `data/` 的备份并监控容量。

Stash 的结构和字段检查不等于原生客户端的加载、连接及路由命中验收；本版尚无 Stash 真机验收结果。Go、Node、Linux 内核与隔离实例的最终验证结果由发布时的验收记录确认，不能据本节推断已完成线上升级。

## 4.13.0

两套规则库的本地原始文件留存与双源详情、自定义分流来源选择，以及 PPanel 四种 MRS 模板来源变体。详见 [4.13.0 更新说明](RELEASE-4.13.0.md)。

## 4.12.0

独立自定义分流订阅、规则选择与节点范围、固定链接及独立管理页面。详见 [4.12.0 更新说明](RELEASE-4.12.0.md)。

更早版本的维护记录见 [README 可靠性说明](README.md#可靠性)。
