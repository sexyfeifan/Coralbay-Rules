# CoralBay Rules 4.15.0

本版新增独立的妙妙屋X 页面，把 666OS / YYDS 规则提供为完整 Clash V3 模板或可单独导入的规则集。入口为“客户端配置 → 妙妙屋X 模板”（`/miaomiaowu`）。详细操作见[妙妙屋X 使用指南](docs/miaomiaowux.md)。

## 使用变化

- **完整模板**：下载 YAML 或复制正文，导入妙妙屋X“模板管理”，再绑定自己的订阅。保留 33 个 MRS 规则集、17 个业务策略组及原序的 28 条 `RULE-SET` 与最后一条 `MATCH`；节点由妙妙屋X 注入。
- **单独规则集**：从 33 项 YYDS 规则中选择一项，按需生成 `payload` YAML。在妙妙屋X“规则集管理”中新建规则集，选择“手动维护”，粘贴正文或从文件导入。
- **来源选择**：两个入口独立选择本机镜像或同版本上游源。“本机”指 CoralBay 服务器。模板选项控制客户端读取 MRS 的位置，规则集选项控制生成时读取的原件；转换后的 YAML 由 CoralBay 提供。
- **预览与下载**：支持正文、链接和 provider 引用片段复制。大规则集最多显示前 200 条，复制与下载保留完整内容；切换来源或条目立即清空旧结果，读取失败不残留旧有效链接。

## 兼容范围

模板将节点层适配为“全球手动”“全球自动”“故障转移”三个组，与业务组合计 20 组。不复刻原版多层地区组、地区均衡或美国／新加坡等地区优先级。该格式面向妙妙屋X 的 Clash V3 模板系统及支持 MRS 的 Mihomo 客户端。

单独导出的规则集使用 `format: yaml`、`behavior: classical`。在模板中替换原 MRS 时，须同时修改 `url`、`format`、`behavior` 和缓存 `path`；页面提供可复制的引用片段。若把文件导入妙妙屋X 托管，还应将引用的 URL 改为妙妙屋X 的公开地址。

模板与规则集使用固定版本。CoralBay 同步新规则后，须重新获取模板或生成规则集，再导入妙妙屋X；旧 URL 不会自动切换到新提交。PPanel、MihomoPro、普通转换和自定义分流继续使用各自的数据与来源设置，已有节点订阅和签名链接不会被批量改写。

## 验证与边界

Go race / vet、前端状态与脚本回归、桌面及手机布局检查通过。来源竞态、错误状态、回环开发预览地址校验以及大规则集仅截取屏幕预览的行为均有定向回归覆盖。

实际导出的本机、上游模板通过官方 V3 插件的 **6 个合成节点场景**；每份保持 **20 个非空组、33 个 provider、29 条规则**。六份渲染结果均通过 Mihomo v1.19.30 的语法检查及运行时 MRS 加载。

全部 **33 份 classical YAML** 被真实内核加载，条数逐文件与导出结果一致。逐项重编码后，与原 MRS 解压内容除计数头外完全相同，核对了域名和 IP 区间的匹配语义。Google 规则的上游首次获取结果也与本机 YAML 摘要一致。具体规则版本、计数差异及验证环境见[适配与验证记录](docs/reference/miaomiaowux-template-format.md#验证边界)。

内核运行验证在隔离网络中使用真实规则文件与合成节点，验证模板渲染及规则加载；未连接真实代理节点，也未在用户的妙妙屋X 私有后台完成上传、绑定及生成订阅操作。

## 备份与升级

升级前备份 `.env`、`compose.yaml` 和**整个 `data/`**。新增模板保存到 `data/rule-templates/miaomiaowu/`，规则集保存到 `data/miaomiaowu/rulesets/`；仍依赖已有规则原件、解码缓存、密钥和数据库，不能只备份新增目录。

确认 Docker Hub 已提供本版镜像后执行：

```bash
sudo env CORALBAY_IMAGE=sexyfeifan/coralbay-rules:4.15.0 rules update
```

固定旧标签的服务器需要显式指定新 `CORALBAY_IMAGE`。升级后登录控制台，确认运行版本和 666OS 资源状态，再打开妙妙屋X 页面。首次生成规则集可能需要约 20 秒。

完整搭建、备份和回退步骤见[部署指南](docs/deployment.md)。GitHub Release、Docker 镜像发布与生产实例升级分别核对；发布文档不代表某台现有服务器已经升级。
