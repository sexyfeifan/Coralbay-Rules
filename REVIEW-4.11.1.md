# v4.11.1 统一订阅管理

- 将“最近生成记录”和“链接管理与拉取统计”合并为订阅转换页内唯一的“订阅管理”。
- 以转换参数标识订阅，忽略签名差异；重复生成不再重复显示统计，展开后可查看保留的各次生成记录和设置。
- 同行提供复制、打开、复用设置、停用/恢复；“更多操作”包含更新签名和连通性抽测。
- 删除一条生成记录、清空生成记录和清理过期统计均保留原有独立语义，不撤销订阅、不重置停用状态。
- 清除生成记录后订阅仍显示，仍可从当前链接复用参数。单次记录保留上限仍为最近 100 条，不伪称完整终身生成历史。
- 支持名称/编号/协议/客户端搜索、有无生成记录与使用状态筛选、最近生成或拉取优先/最近拉取优先排序、分页。
- 新生成结果自动刷新统一列表。后端先合并检索、排序再分页，避免只处理当前页导致漏项。
- 不变更节点、分组、规则、签名算法或现有 SQLite 数据结构。

验证：重复生成聚合、保留当前签名、名称检索、分页排序、删除单条/清空历史后统计及停用保留、旧界面去重等回归；Go 竞态测试、JS 语法、Shell 语法和差异检查。
