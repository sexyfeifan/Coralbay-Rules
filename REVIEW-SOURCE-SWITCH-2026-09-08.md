# 分流来源切换：专项审查与修复建议

日期：2026-09-08。代码基线：4.13.0，提交 `9fee0ed1f0d668f4fb46712f28d7b9b9e2069175`。

本文件保留 4.13.0 审查时的发现。九项问题现已纳入 4.14.0 修复与回归，结果见 [实施记录](docs/plans/2026-09-08-v4.14.0-implementation.md)。以下描述审查当时状态：没有修改应用代码、线上数据或部署；原有规划文档的未提交修改保持不动。验证使用隔离数据、临时 Go overlay、Ruby 和 Node 样例。本文不据此推断线上已经发生故障。

## 优先级

- P1：优先修复，会使受影响场景无法按说明完成使用。
- P2：在统一来源切换发布前修复，涉及条件性失败、恢复能力、兼容性或错误状态说明。

## 已确认问题

### 1. P1：MihomoPro 内置备用配置无法注入节点订阅

- 位置：[覆写模板](templates/openclash/MihomoPro_overwrite.conf#L14)、[内置完整配置](templates/openclash/Pro_cn.upstream.yaml#L43)、[同步回退](sync.sh#L98)。
- 触发：YYDS 下载失败，使用镜像附带的完整配置，然后按说明填写主／备用节点订阅。
- 原因：覆写只修改“优质服务商／备用服务商”的 URL；内置配置的 `proxy-providers` 为空，没有这两个定义。
- 验证：解析内置配置的节点段，执行对应修改条件前后均为空。[OpenClash 官方函数](https://raw.githubusercontent.com/vernesong/OpenClash/master/luci-app-openclash/root/usr/share/openclash/ruby.sh)仅在目标映射和键已存在时修改。
- 修复：创建完整的节点订阅提供者定义，统一名称，并处理未填写备用订阅的情况。验收应确认实际读到节点并加入策略组。

### 2. P2：普通转换可能交付被截断的配置

- 位置：[转换结果读取](main.go#L481)、[结构检查](main.go#L501)。
- 触发：转换后端返回的配置超过 16 MiB，截断处仍构成合法 YAML。
- 验证：临时测试中，16,777,278 字节结果被截成 16,777,216 字节，末尾规则及兜底全部丢失；函数仍返回成功，节点数量仍为 1。
- 修复：读取上限加 1 字节，明确拒绝超限结果；超过限制不能当成完整响应交付。完整分流输出还应验证规则层与必要兜底，纯节点输出另行处理。
- 相关加固：[远程配置缓存](remote_configs.go#L122)也应拒绝超过 2 MiB 的内容，并在覆盖有效缓存前验证配置格式，避免保存截断文件或 HTTP 200 的错误页面。这项属于代码检查建议，未做远端错误页面的端到端复现。

### 3. P2：只有 MihomoPro 上游配置变化时，新配置可能被丢弃

- 位置：[发布版本标识](sync.sh#L69)、[同版本处理](sync.sh#L303)。
- 触发：666OS release、geo、程序版本、域名都未变化，但 YYDS 配置变化；或上一次使用内置回退，本次恢复上游下载。
- 原因：发布标识不包含实际 MihomoPro 配置摘要。相同标识的目录已经存在时，新生成目录被删除，继续使用原目录。
- 修复：将实际完整配置源摘要纳入版本身份，保留配置来源和规则来源各自的追溯信息。
- 验收：两轮同步仅改变完整配置输入，第二轮必须交付新内容；回退恢复也应生效。

### 4. P2：MetaCubeX 资源清单损坏后，反复同步无法修复

- 位置：[清单发布](routing_rule_resources.go#L130)、[快照写入顺序](routing_rules.go#L553)。
- 触发：`resources.json` 损坏，但原始 YAML 和经过摘要保护的规范化快照仍然完整。
- 验证：截断清单后连续两次显式同步都拒绝重建；固定下载和使用集合的构建持续不可用。失败前还会留下新的候选 JSON。
- 修复：显式修复时，根据已验证的快照与原件重建清单，验证通过后原子发布；诊断保留损坏清单，清理未发布的失败候选。
- 验收：损坏清单可修复，损坏原件不会被误信，其他已交付版本保持可用。

### 5. P2：版本检查接口故障会阻止已知版本的原件补齐

- 位置：[版本解析失败分支](routing_rules.go#L433)。
- 触发：旧规范化快照完整，某个原始 YAML 缺失，GitHub 版本接口不可达，但固定提交的文件地址可达。
- 验证：显式同步只请求版本接口，不请求已知提交的文件地址，缺失原件没有恢复。
- 修复：原始文件必需的路径要检查原件完整性；无法获知最新版本时，允许修复已知且经过验证的提交，同时保留版本检查失败提示。

### 6. P2：666OS 本地详情可能把损坏文件标为可用

- 位置：[详情读取](legacy_rule_resources.go#L405)、[缓存状态](legacy_rule_resources.go#L481)。
- 原因：详情根据文件非空标记已缓存，未复用固定下载的大小、摘要校验。
- 验证：将已留存的规则改成 7 字节损坏内容，固定下载返回 503，详情却返回 200、`cached:true`，并展示同一不可用地址。
- 修复：详情、目录、下载统一使用相同资源校验；关联 geo 文件也应按自身清单核验。缺失、损坏、未检查必须有不同状态。

### 7. P2：MetaCubeX 差异摘要可能拿旧摘要代替已读取原件

- 位置：[前端差异摘要](web/app.js#L93)。
- 触发：本地原件缺失，仅保留旧快照摘要；旧摘要与上游摘要相等。
- 验证：Node 样例中，即使响应明确原件不可用、不可比较，页面仍显示“已读取文件的 SHA-256 相同”。
- 修复：仅比较确实读取并校验过的原始文件；否则显示不可比较及原因。旧摘要可以展示，但不能当成原件可用的证明。

### 8. P2：部分本机规则会被普通转换依赖统计判成外部来源

- 位置：[依赖来源判断](remote_config_dependencies.go#L91)。
- 原因：目前仅将同域名 `/_converted/` 路径算作本机，其余路径均计为外部。
- 验证：隔离数据中的同域名 `/surge/AI.txt` 确实存在，分析仍返回本机 0、外部 1。
- 修复：依据受管理资源清单判断来源和可用性，覆盖本机原生及固定版本资源；未知路径明确标为待确认，不能直接作为“全量镜像”的判定依据。

### 9. P2：部分 Stash 原生节点字段会导致整个订阅被拒绝

- 位置：[字段白名单](routing_builder.go#L786)、[拒绝未知字段](routing_builder.go#L803)、[Stash 输出](routing_builder.go#L1115)。
- 验证：使用 Stash Hysteria2 的 `auth/up-speed/down-speed` 或 TUIC v5 的 `version: 5`，输入分别被拒绝为不支持 `auth`、不支持 `version`。
- 修复：输入统一转换为内部字段，检查别名冲突及协议版本；输出再按客户端规范生成。[Stash 官方协议文档](https://stash.wiki/proxy-protocols/proxy-types#hysteria2)。
- 边界：当前 HY2 输出仍使用 `password/up/down`，与文档字段不同；是否存在兼容别名尚未原生验证，不能据此断言全部连接失败。Stash 原生加载与命中仍需验收。

## 本轮未发现的确定问题

来源切换改变已选规则内容、自动混用两套规则库、批量覆盖旧方案等问题，本轮未发现确定证据。来源等价与链接隔离的针对性检查通过，集合校验确实核对了内核实际加载数量。这不等于所有客户端或所有异常场景均已经验证。

## 建议实施顺序

1. 先修节点注入、超限拒绝、同步遗漏和镜像恢复，形成独立修复版本。
2. 统一资源身份与健康状态，消除详情、下载、差异与依赖统计之间的矛盾。
3. 接入四个功能的统一来源控件，保留旧方案默认行为；配置文件来源与分流规则来源独立。
4. 按客户端开放能力。无等价上游文件的派生格式继续说明原因；节点格式兼容必须经过验证。

## 新功能发布门槛

- 相同版本切换来源，规则内容、优先级和策略保持一致；更新版本单独执行。
- 本机副本缺失或损坏时明确阻止相应生成，能够修复，不静默变更来源。
- 上游不可达时，本机规则仍能读取；测试区分节点订阅访问与规则上游访问。
- 预览、保存、客户端下载三步使用同一份通过验证的结果；客户端更新后确实读取新来源。
- 同一规则连续两次切换、并发更新、失败重试、回退恢复都加入回归。
- 记录实际规则来源、资源版本与健康状态，提供最近有效版本恢复入口。
- 历史资源按引用保留，并增加磁盘用量提醒；不能直接按时间删除仍被订阅引用的文件。
- 发布核对 GitHub 提交、Docker 镜像和运行版本。此前自动镜像发布因缺凭据失败，需补齐自动发布配置并实际验收；手动推送成功和自动流程成功分别记录。

现有测试通过证明了已覆盖场景；本轮发现的异常恢复、节点注入及边界场景应补入后续验收，不能只复用上一版的通过结果。
