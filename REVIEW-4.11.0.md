# v4.11.0 实施与验收报告

## 已实施的上一轮计划

1. **独立签名**：订阅使用随机 256 位持久密钥和 v2 签名；管理员密码与操作令牌变更不影响新链接。首次升级时保存旧签名密钥，旧链接兼容 90 天。链接管理提供“更新签名”，保持转换参数、计数和停用状态，复制新链接后在客户端替换。过渡期到期拒绝旧签名。
2. **预设隔离与校验**：只有本站内置 MihomoPro 完整配置触发 Stash 的固定分流改造；第三方预设、仅节点输出不被注入该规则。Clash/Stash YAML 增加重复名称、策略/节点/规则集引用及循环引用校验。
3. **网络边界**：拉取订阅补全字段、远程预设及转换容器的出站统一限制公网 IP；验证 DNS 后直接连接该 IP，重定向重新检查，拒绝私网、回环、链路本地、CGNAT、保留网段及 IPv6 绕过。转换容器在 Docker internal 网络，强制经认证出站代理，代理端口不映射到宿主机。必须更新 Compose 才有容器隔离，仅拉取应用镜像不够。
4. **地区互补**：Mihomo/OpenClash 和订阅预设的地区过滤来自 internal/regions；其他地区使用同一合集的 exclude-filter，JP、HK 等纯代码及未知地区不会因排除表达式不一致而漏选。生成器可重复运行，保留全局手动兜底。多地关键词名称仍可能同时匹配多个已知地区，这不是物理归属认证。
5. **真实调度时间**：下一次计划时间写入 schedule.json，查询不再推迟显示时间，重启沿用未完成的调度截止时间。
6. **验证含义与实测**：前端使用“可解析节点”。新增服务器侧连通性抽测：使用固定版本 Mihomo 内核、临时最小配置、仅环回监听，最多检测前 3 个内联节点的 Google HTTPS 204 请求。支持 Stash/Clash 输出里的 VLESS、VMess、SS、Trojan；不支持的字段明确提示，不改写用户订阅。拒绝私网节点、插件及链式拨号。服务器实测不代表 iOS Stash/Loon/Surge 实机认证，也不保证所有网站可访问。
7. **事务统计与保留边界**：SQLite WAL 持久化，旧 JSON 只迁移一次并保留备份；坏数据拒绝迁移。索引分页、按协议/编号/客户端检索、活跃和停用过滤；每条链接一条聚合记录，不记录逐请求日志或访问 IP。登记上限 10000 条，达到上限拒绝新登记而不是删除停用记录。“清理过期统计”只重置 180 天未访问的已启用链接计数，保留登记及所有停用记录。
8. **统计语义**：界面明确“最近访问”不是在线状态；成功次数仅表示服务端写出响应，不保证客户端接收或导入；浏览器打开也计数。历史与链接登记独立。

## 复查额外修复

- 首屏分页变量初始化顺序错误：首次进入链接管理不再因暂时性死区报错。
- 历史保存失败不再假装成功；新链接在返回之前登记到持久数据库。
- 源 VLESS URI 缺少用户信息时不再触发空指针异常。
- 规则生成器重复执行不会生成 exclude-exclude-filter。
- 诊断读取停用状态失败时拒绝执行；不会绕过坏数据库。
- 构建上下文排除 settings 和 .env，避免意外带入本地密钥。

## 验证

- Go 单元/竞态回归、JS 语法、Shell 语法、git diff 空白检查。
- 回归覆盖旧签名过渡、修改密码、篡改拒绝、JSON→SQLite 一次迁移、重启与停用保留、删除历史、重新签名、计数并发、分页、预设隔离、引用检查、地区覆盖、调度重启、诊断危险字段拒绝。
- 隔离 Docker 联调：真实源转换 6 个节点；前 3 个节点 HTTPS 204 抽测成功；清空测试历史不解除停用，重新签名也不解除停用。
- 隔离容器直接外网连接失败；经受控代理转换成功；元数据地址被拒绝。
- 浏览器可视化回归未完成：浏览器连接调用被中断。接口联调和前端语法测试不能替代完整视觉验收。

## 部署、备份与边界

### 本次生产验收（2026-08-26）

- 生产服务已运行 4.11.0，容器健康；规则同步产物 generator_version=4.11.0，33 个规则文件校验通过。
- 原有签名订阅返回 HTTP 200，包含节点和规则集；新签名链接解析出 6 个节点。
- 生产服务器抽测前 3 个节点均完成 HTTPS 204 请求，本轮延迟分别为 344 / 236 / 348 ms（瞬时样本，不代表持续质量或手机端性能）。
- 旧签名兼容截止时间：2026-11-24 18:41（北京时间）；管理页可更新签名。
- 连续读取显示同一个 next_sync_at，未出现查询导致的时间漂移。
- converter_private 网络 internal=true；移除代理环境变量后直接出站失败；通过受控代理访问元数据地址失败。
- 生产备份：/opt/coralbay-rules/backups/pre-4.11.0-20260826/state.tar.gz。rules / 666 管理脚本均已升级。
- Docker Hub 的 4.11.0 / latest 已发布 amd64 + arm64，索引摘要 cbfbb5064d6b8e46d115f50212e2c385e3121d62d5a459e05652744267f29608。
- GitHub CI 全部通过（运行 32958763677）。独立 Docker 自动发布任务因仓库未配置 DOCKERHUB_USERNAME / DOCKERHUB_TOKEN 失败；本次已从本机完成镜像发布，不应将自动任务标为成功。未来要启用 GitHub 自动发布，需另行配置专用凭据。

使用最新 install.sh 的“升级”操作会更新 Compose。保留 /data/settings（密钥和 SQLite/WAL）、/data/schedule.json、安装目录 .env 和 compose.yaml。在线复制单个 SQLite 文件不可靠，备份时停应用或使用 SQLite 备份接口。

旧 JSON 仅是升级前备份，不是新数据库镜像。不要直接降级到 v4.10 并沿用旧 JSON，否则升级后新增的停用记录可能丢失。回退应停止应用并恢复对应版本的完整备份。

Fake-IP DNS（198.18/15）会被安全策略拒绝；部署服务器应使用真实公网解析，不应关闭内网限制来兼容 Fake-IP。

诊断使用原版 [Mihomo](https://github.com/MetaCubeX/mihomo) v1.19.30 二进制，固定多架构镜像摘要 baf38d282b785d7037337676714a69e3fdd1f2d9bf748dfd25fce681a624ea74，遵循其 GPL-3.0 许可；不是 Stash 内核。

## 后续产品迭代（非本次已完成承诺）

- 建立固定版本 iOS/macOS 客户端实机兼容性矩阵；当前不能声称所有协议和设备均已实机认证。
- 达到一万条登记前设计管理员归档/导出和扩容迁移，不能通过清理停用名单释放容量。
- 将三节点同步抽测演进为持久诊断任务，支持队列和取消，避免长请求受反向代理超时影响。
