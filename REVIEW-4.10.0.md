# v4.10.0 订阅管理与代码复查

## 已完成

- 独立持久化链接登记、成功响应次数、转换/写入失败次数、停用拦截次数、首次/最近拉取时间和粗粒度客户端标识，不存储访问 IP。
- 管理台提供刷新统计、复制链接、停用/恢复。48 小时活跃窗口只是请求时间判断，不是设备在线判断。
- 清除/删除历史前迁移链接登记；删除历史不清空统计或解除停用。相同转换参数共享状态。
- 停用在转换前检查；已开始的转换可能完成，已下载配置无法撤回。
- 存储损坏时返回 503，不把损坏的停用名单当作空名单。
- 修复单条历史删除写入失败却返回成功的问题。
- 并发计数、进程重启、历史清空后停用保留、损坏状态回归测试。

## 复查发现与进化计划（尚未实施）

1. P1：订阅签名与管理员密码/操作令牌耦合。修改密码会让旧链接失效。应迁移到独立订阅签名密钥，使用版本化签名并明确旧链接过渡期。
2. P1：Stash 后处理无条件注入固定规则源；自定义远程预设可能被覆盖，缺少相应策略时会出现悬空引用。应仅针对内置预设启用改造，并校验所有规则指向的组。
3. P1：源订阅拉取、重定向、DNS 解析及转换后端出站需要统一 SSRF 防护；单纯 URL 校验不足以防止重绑定与跳转至内网。
4. P1：Mihomo/OpenClash 的“其他地区”排除表达式与主地区识别表达式不完全一致，纯 JP 等代码命名可能重复入组。应从一份区域定义生成互补匹配规则，并用多国/未知名称测试覆盖率。
5. P2：next_sync_at 每次查询都用当前时间加间隔，不能表示真实计划执行时间，应持久记录上次执行和调度截止时间。
6. P2：节点数量和结构检查不等于真实可连接，UI 的“可用节点”措辞应改为“可解析节点”，增加可选端到端测试。
7. P2：统计目前每次请求原子重写 JSON，适合单实例低流量；规模增大应使用 SQLite 事务、分页查询、管理检索与有界保留策略，停用记录不可随统计清理删除。
8. P2：统计只记录响应写出结果，不保证手机已接收/导入；旧历史清除前从未登记且从未再拉取的链接不可枚举。浏览器预览也计入次数；UA 不是可信设备身份。

## 验证范围

Go 回归/竞态测试、JS 语法检查及服务接口验证。不是所有客户端实机互通认证；需要后续建立 Stash/Loon/Surge/Mihomo 固定样例兼容性矩阵。
