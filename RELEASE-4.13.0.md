# CoralBay Rules 4.13.0

两套规则资源现在都可保存和查看本机副本。新分流方案可以选择从 CoralBay 本机或同版本上游下载规则，旧方案继续保持原有交付行为。

## 规则资源与页面

- MetaCubeX 保存 78 项原始 YAML、SHA-256、版本和规范化快照，首次启动补齐，随后独立每日同步；失败保留上一有效版本。
- 两个资源页提供本地与上游详情、条目搜索和分页、版本与文件摘要；上游预览与本地发布分开，查看本地不会触发联网同步。
- 666OS 提供原始 MRS 元信息及关联 geo 可读源，明确两个分支版本独立。缺少可读源时不把其他集合冒充原文。
- MetaCubeX 差异显示已读取文件的版本与摘要；666OS 可进一步查看关联 geo 文本的新增和删除，但不将其视为 MRS 的精确反向还原。

## 来源选择与兼容

- 新建自定义分流方案默认 CoralBay 本机，也可选择上游。生成规则集合的固定版本地址，两种模式保持相同规则、策略和优先级；缺少本机原件时明确报错，不静默回退。
- 来源设置属于各自方案；预览和保存记录包含实际来源、版本、生成时间及逐规则摘要。旧方案缺少新字段时保持内嵌，主动切换并保存成功后原分流订阅链接不变。
- Clash（Mihomo 内核）、Mihomo、OpenClash、Stash 的 PPanel 原始 MRS 模板提供本机／上游变体。原共享模板地址与内容保持原义；派生格式无等价上游文件时不提供虚假的地址切换。
- 普通转换保留签名链接和历史数据。INI 来源明确标为配置文件来源，并展示缓存配置声明的规则依赖；本机、外部、缺失与未识别依赖分别计数，不宣称第三方配置已完全离线化。

## 验证

- Go 回归与分模块竞态测试通过；覆盖来源切换、旧方案迁移、原始资源校验、缺失与损坏、上游失败保留有效版本，以及检查／同步隔离。
- Linux 内核验证实际执行配置检查与规则加载数量核对。真实 MetaCubeX 提交 `c9f1fd664797ccdc85783c5746827a3f22ce6068` 的 78 个集合共加载 161,732 条，与各原始文件逐项一致。
- 合成规则分别通过本机与上游 URL 下载，验证缓存摘要、域名、关键词、含逗号量词的正则、IPv4/IPv6、直连优先级和兜底。配置检查放行但内核漏载的无效正则，会被额外加载数量校验阻止发布。
- 本地隔离预览完成 666OS 33 / 33、MetaCubeX 78 / 78 初始化，并验证真实资源详情和来源选择。

Stash 按官方规则集合语法生成并完成结构、引用检查；本轮没有 Stash 原生客户端环境，尚未完成 Stash 真机加载与实际命中验收。Mihomo 内核结果不能代替这项验收。

## 升级与数据

升级前备份 `.env`、`compose.yaml` 和整个 `data/`，具体步骤见[搭建与升级指南](docs/deployment.md)。

新增固定资源位于 `data/rule-resources/666os/` 与 `data/routing/rules/releases/`，不受旧 666OS 三版本清理影响。当前保留已交付资源版本，请关注磁盘空间；手动删除可能让旧配置引用的文件失效。

首次升级会补齐旧 MetaCubeX 快照缺少的原始文件。两套规则目录、更新任务和旧普通订阅数据继续隔离。

本版完成[升级规划](docs/plans/2026-09-08-rule-source-selection.md)的本地资源、双源详情、订阅来源选择，以及配置依赖识别。完整历史管理、手动固定历史版本、快照包导入导出和第三方 INI 嵌套依赖全量镜像仍属后续阶段。
