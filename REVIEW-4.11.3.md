# v4.11.3 可靠性与安装流程修复

## 已修复

- 同步与 HTTP 回滚统一使用 `/data/.publish.lock` 的内核排他锁。异常退出不再留下必须手动删除的目录锁；回滚遇到正在发布的任务返回 409，避免与目录清理交错。
- 修复登录限流键包含连接端口的问题，同一来源 IP 的新连接共用限制，支持 IPv4 与 IPv6。反向代理部署按可信的直接连接来源限流，不直接采信任意 `X-Forwarded-For`。
- 已删除或停用的订阅重新生成相同参数时返回 409，提示恢复，保留原状态与统计，不再交付 410 链接。有效订阅重新登记时保存当前签名地址。
- Clash、ClashR、Stash 的节点计数改为 YAML 解码，支持行内列表、无缩进列表和不同字段顺序。
- 独立 Shell 定时循环每轮启动独立的 `once` 进程，避免条件调用函数导致 `set -e` 失效。失败下载不会继续进入发布阶段。
- 版本或域名与当前产物不一致时，应用启动立即安排同步。同步脚本尊重 `DATA_DIR`。
- HTTP 服务增加请求头读取和空闲连接超时；缺少管理员密码或操作令牌时明确拒绝启动。

## 一键安装与升级

仓库已有 `install.sh`。它安装 CoralBay 的 Compose 服务，要求 Linux、Docker Engine、Compose 插件、curl、coreutils 和已有 HTTPS 反向代理；不自动安装 Docker 或申请证书。

- 首次安装要求隐藏输入并确认至少 12 位密码，不再采用公开固定默认密码。已有密码保留，避免静默更改用户凭据。
- `.env` 仅解析已知配置字段，不再作为 root Shell 代码执行，也不沿用上一次菜单操作加载的其他安装配置。
- 校验域名标签、独立绝对安装路径、端口所属容器和安装目录。自定义路径记录到 `/etc/coralbay-rules/install-dir`。
- 下载新管理脚本后先做语法检查并原子替换；升级由新脚本接手。旧快捷命令仅在确实指向本项目时删除。
- 候选 Compose 先校验和拉取镜像，成功后备份并替换配置，避免拉取失败破坏原配置。每份备份使用唯一目录。
- 启动后验证应用和订阅后端；失败返回错误并恢复已有配置，不再显示成功。镜像本身不自动回退；数据不会删除，配置备份不等于完整数据备份。
- 重配置选择的同步间隔覆盖旧网页设置，启动失败时恢复旧间隔与时间计划。
- 无交互输入时明确失败，避免必填提示无限循环。支持 `rules password`。

## 验证

- `go test -race ./...` 与 `go vet ./...`。
- 新增登录跨连接限流、停用/删除后重新生成、YAML 多种写法、跨进程锁与强杀释放、回滚冲突回归。
- `bash tests/install_test.sh`：域名校验、环境文件拒绝执行、配置隔离、EOF、Compose 渲染、拉取与启动失败保护、同步间隔持久化。
- Linux 容器中运行 `bash tests/sync_test.sh`：遗留目录锁兼容、下载失败退出、独立循环失败保护、发布锁冲突。
- 实际运行应用与隔离网络的 subconverter，使用 Linux 原生 Docker 数据卷：登录、后端版本探测、Shell 持锁时 HTTP 回滚返回 409、强杀释放后回滚成功、公开状态文件切换。
- Shell 和前端 JS 语法、`git diff --check`、Docker 镜像构建。

这些检查未替代真实 VPS 上的系统安装、域名证书配置和各客户端完整实机订阅测试。

数据目录应使用支持 Linux `flock` 的本地文件系统。macOS Docker Desktop 的共享绑定目录在本次测试中未能可靠保持 Shell 子进程取得的文件锁；改用 Linux 原生数据卷后互斥与强杀释放检查通过。安装脚本明确只支持 Linux 服务器，不把 macOS 共享目录或网络文件系统视为已验证部署方式。

## 发布

沿用 `sexyfeifan/Coralbay-Rules` 和 Docker Hub 的 `sexyfeifan/coralbay-rules`，版本 4.11.3，支持 Linux amd64 / arm64。

发布工作流新增发布前验证，修复手动执行时版本号变成 `main`、Tag 发布未更新 `latest` 的问题。检查时仓库未设置 `DOCKERHUB_USERNAME` / `DOCKERHUB_TOKEN`，这是此前 Docker 工作流失败的原因；自动发布仍需仓库所有者配置这两项 Secrets。本次可使用本机现有 Docker Hub 登录直接构建推送，无需把本机凭据复制到 GitHub。
