# 4.12.0：自定义分流订阅

日期：2026-09-08。v4.12.0 已完成代码发布、多架构镜像推送与生产升级；以下记录本版功能与实际验证边界。

## 新功能

- 新增 `/routing`：输入 1–8 个原始订阅，选择服务规则及节点范围，生成 Mihomo、OpenClash、Stash 的完整 YAML 配置。
- 提供 78 类 MetaCubeX 规则，支持分类搜索、推荐组合、全局地区与正则筛选、逐规则覆盖、批量设置、直连/代理/拦截和 MATCH 兜底。代理策略支持手选、自动测速、故障转移。
- 预览实际节点、策略组、生成器的真实规则顺序与最终 YAML；空组、无法保持语义的协议字段及无效引用会阻止保存。
- 方案保存后提供固定订阅地址、二维码和下载。修改方案继续使用原地址；支持复制方案、停用、恢复、重置交付令牌和拉取统计。
- 桌面使用分组侧栏，手机使用抽屉。订阅管理独立成页，保留原有 hash 入口；分流页面按需加载，与旧业务 API 分开初始化。

## 数据与更新

新方案、统计和规则快照保存在 `$DATA_DIR/routing`，不迁移旧订阅，不改写旧 `/sub` 签名、历史、666OS `current` 或同步/回滚目录。MetaCubeX 使用独立同步过程，同一次构建固定到同一规则版本；classical 域名规则与 IP 规则直接内嵌到配置，不依赖 Nextin。

客户端更新同一地址时应用已保存方案。配置构建缓存为 5 分钟，失败刷新返回错误并实施 30 秒重试冷却；规则缓存 24 小时，可手动同步。规则更新失败保留本模块上次有效快照并显示警告。

升级前应备份安装配置和完整数据目录，包含 SQLite/WAL 与签名密钥。管理脚本自带备份仅覆盖部署配置；失败升级不会自动回退已拉取的镜像。

## 支持与验证边界

完整分流输出限于 Mihomo / OpenClash 和 Stash。输入支持带内联 `proxies` 的受支持 YAML，以及 SS、VMess、VLESS、Trojan、Hysteria2 的受支持 URI/Base64；每个源最多 8 MiB，合并最多 5,000 个节点。不展开远程 `proxy-providers`，不继承输入配置中的规则、DNS、脚本和策略组。各协议字段仍须通过目标客户端校验。

实现阶段已通过完整 Go 竞态测试、Vet、前端语法、隔离镜像启动、桌面和手机浏览器流程，以及 Mihomo 无网络配置语法校验；78 个真实规则文件解析通过，共 161,729 条。详细过程保留在 [预览验证记录](REVIEW-4.12.0-preview.md)。

这些验证未使用生产订阅建立代理连接，也未完成 Stash 真机验收。地区标签依据节点名称，不表示实际出口或服务解锁结果；配置语法通过不能替代目标客户端中的连接、规则命中、策略切换与订阅更新验证。

## 发布与生产验证

- 源码提交：`12c2631a5ce9907e70c96f0c5196ae1cb46b8555`；[GitHub CI](https://github.com/sexyfeifan/Coralbay-Rules/actions/runs/34143360087) 全部通过。
- Docker Hub：`sexyfeifan/coralbay-rules:4.12.0`、`v4.12.0`、`latest`，支持 Linux amd64 / arm64，包含 SBOM 和 provenance。
- 镜像索引摘要：`sha256:59fd386fba8796d9aa1826eb4b82f1cbec10a352e542550c24dd922aa872bd62`。已从远端注册表及生产服务器分别核对。
- 本次镜像由本机现有 Docker Hub 登录完成推送。仓库尚未设置 Docker Hub Actions Secrets，自动 Docker 工作流在登录步骤失败；这不表示本次手动镜像发布失败。
- 升级前完成配置和完整数据备份；生产服务已运行 4.12.0，应用与更新器健康，订阅后端运行正常。旧订阅记录、签名密钥和配置核对一致，原有 Stash 订阅返回正常。
- 新模块已同步 78 项 MetaCubeX 规则；使用已有订阅完成不保存的预览，成功生成 Mihomo 和 Stash 配置。未建立真实代理连接，也未替代 Stash 真机验收。
- 公网健康检查与新页面 HTTP 200；公开脚本和样式的 SHA-256 与发布源码一致。
