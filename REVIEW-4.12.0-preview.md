# 4.12.0 预览：独立自定义分流订阅

日期：2026-09-07。本文记录当前仓库预览实现与本地验证，不表示正式发布、Docker Hub 镜像已更新或生产环境已部署。

## 功能范围

新增 `/routing` 页面和 `/api/routing/` 管理接口。用户提供现有订阅后，服务提取节点，应用全局与逐规则的节点筛选、代理策略及直连/拦截设置，生成 Mihomo / OpenClash 或 Stash 可更新的完整 YAML 链接。配置内嵌 classical 域名与原生 IP 规则，不需要 Nextin。

方案编辑维持交付链接不变；停用与重置令牌分别阻止对应交付。预览与正式保存都先构建配置；未通过节点、协议字段、空组、规则类型或引用检查的候选不会替换已发布方案。

输入支持带 `proxies` 的原生 YAML，以及 SS、VMess、VLESS、Trojan、Hysteria2 / `hy2` 的受支持 URI/Base64 订阅。原生 YAML 支持 SS、VMess、VLESS、Trojan、Hysteria2、TUIC、HTTP、SOCKS5，具体字段受目标客户端校验限制；不展开远程 `proxy-providers`，不默默忽略无法映射的 URI 扩展。每个方案最多 8 个源、单源 8 MiB、合并 5,000 个节点。暂不生成其他客户端的完整分流配置。

桌面导航改为分组侧栏，手机使用抽屉，保留原有 hash 入口。新增分流订阅、MetaCubeX 规则源与独立订阅管理页面；普通转换和新分流的管理数据分开展示。

## 新旧数据和更新边界

- 78 项目录直接来自 MetaCubeX/meta-rules-dat；其中 76 项域名采用 classical 输入，2 项采用 IP CIDR 输入。BT Tracker 不作为广告或隐私追踪默认拦截。
- 方案与交付统计只写 `$DATA_DIR/routing/routing.sqlite`；规则只写 `$DATA_DIR/routing/rules`。旧 666OS `current`、同步/回滚目录和旧订阅数据库不参与新模块构建。
- 一次规则同步先确定上游提交，再抓取该提交内的所需规则并校验。不可变快照有 SHA-256，状态指针通过原子重命名发布；不混用两个提交。规则更新失败保留本模块有效快照并显示错误，不使用旧 666OS 回退。
- 规则按需缓存 24 小时，也支持手动同步全部目录。GitHub 分支 API 不可达且没有有效缓存时，可使用已核验固定提交，并显示版本检查警告。
- 节点与完整配置构建缓存为 5 分钟；失败刷新返回 503，并保留已存输出供诊断，30 秒内不重复抓取失败上游。客户端建议更新间隔独立于该缓存。
- 方案保存使用 SQLite 事务及版本条件，旧编辑版本会收到冲突；交付时重新检查令牌、停用状态、客户端与方案版本。

## 独立审查与回归

局部审查发现并完成两项资源问题修复：

1. 方案列表原先读取并保留所有完整配置正文，配置较大时容易造成不必要的内存占用。列表、管理详情及交付元数据查询现在从 SQL 层省略正文。
2. 同一方案刷新锁原先无法响应请求取消，慢上游会使取消的请求继续排队。现在使用可取消等待的信号量，并记录失败尝试时间实施 30 秒重试冷却。

新增 `routing_review_test.go` 针对这两项修复检查：大型损坏输出不会破坏元数据列表，损坏内容也不能被当作有效配置交付；等待另一刷新时取消请求能及时返回；连续失败请求只抓取上游一次、给出冷却信息，并保持上一次已存输出不变。

## 本记录已实际完成的局部验证

- `go test ./... -run '^TestRoutingRule' -count=1`：独立规则目录、解析、缓存发布、旧目录隔离、失败保留、来源 URL 边界和处理器测试通过。
- `go test -race . -run '^TestRoutingRule' -count=1`：上述规则模块竞态检查通过。
- 通过正常 TLS 下载固定提交 `8964c30fc18a52dfc6761d30c664faaac0c219b1` 下全部 78 个真实规则文件，并使用生产解析器执行本地 corpus 测试：78/78 通过，共 161,729 条。包含关键词、带逗号量词的正则以及 IPv4/IPv6，无静默丢弃。
- `go test . -run '^TestRouting(MetadataDoesNotLoadCorruptLargeOutputs|CancelledDeliveryDoesNotWaitForProfileRefresh|RefreshFailureCooldownPreservesPreviousOutput)$' -count=1`：新增三项审查回归通过。
- 同一组三项测试增加 `-race` 后通过，未发现竞态。

普通测试使用本地模拟 HTTP 响应，不依赖外部网络或真实订阅凭据；corpus 测试读取事先下载的固定提交文件。

## 整体验证

- `go test -race ./...`：完整项目测试通过，包含旧功能回归及新模块的创建、更新、固定地址刷新、停用、令牌重置、并发保存和数据隔离。
- `go vet ./...`：通过。修改的前端脚本通过 Node 语法检查；新页面启动与切换旧页面的模拟检查确认延迟加载旧业务 API。
- `docker build -t coralbay-rules:routing-preview --build-arg VERSION=4.12.0-preview .`：通过；本地镜像已包含独立规则目录和新页面资源，未推送。
- 最终镜像 `sha256:a49b138a99d1025a5df06f0d0fb7f042ffbd88680309a25053fa504f85368283` 使用全新临时数据和仅绑定 localhost 的端口启动通过；健康检查、匿名 API 拒绝、随机密码登录、安全 Cookie、认证后的目录/地区/方案接口、新页面资源均正常。访问本机订阅源被拒绝；内置 Mihomo 对合成配置执行 `-t` 通过。此次创建的容器、网络、临时数据和演示凭据已清理。
- 桌面浏览器实际完成创建方案、导入 3 个演示节点、推荐规则预览、OpenAI 独立筛选美国节点、保存并显示 Mihomo/Stash 固定地址。预览确认 OpenAI 组为 1 个节点，其他代理组为 3 个节点。
- 浏览器规则源页面完成全部 78 项同步，并查看 OpenAI 的 23 条规则与固定提交来源链接。浏览器使用预先核验的真实规则文件和合成节点，未使用生产订阅。
- 390 × 844 手机视口实际检查方案卡片、抽屉导航及从抽屉进入独立规则页。修正关闭抽屉后仍可聚焦隐藏菜单的问题。
- 使用固定版本 Mihomo 容器在 `--network none` 下执行配置 `-t` 校验，复杂测试配置（Reality、正则、IPv6 和逗号节点名）通过；没有建立实际代理连接。

临时浏览器认证与演示订阅注入只存在于本地测试进程中，测试入口文件已移除，不进入生产代码或镜像。本地演示页面只供界面试用；其中的模拟节点不能用于实际代理连接。

## 发布与验收边界

当前未部署生产，也未进行 Stash 真机验证。节点地区基于名称识别，不代表实际出口所在地或服务解锁结果。仍需在目标客户端验证配置导入、实际连接、规则命中、策略切换及同一订阅地址的更新行为；语法通过不能代替这些运行验收。

本次不迁移旧订阅，不接入解锁检测，不生成通用节点 URI 所无法承载的完整分流配置。上游来源说明和许可证保存在 `docs/reference/metacubex-routing-source-notice.md` 与同目录许可证副本中。
