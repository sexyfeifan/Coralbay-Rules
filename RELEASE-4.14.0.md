# CoralBay Rules 4.14.0

本版统一四个入口的分流规则来源选择，并修复来源显示与实际产物不一致、损坏资源难以恢复、转换结果被截断等问题。完整范围见[实施细则](docs/plans/2026-09-08-v4.14.0-implementation.md)，搭建和升级见[部署指南](docs/deployment.md)。

## 使用变化

- **MihomoPro 覆写**：本机镜像／上游源分别提供同版本、相互匹配的覆写与完整配置。切换后重新导入对应覆写，专属配置文件会刷新。主、备用订阅能建立完整节点提供者。
- **PPanel 模板**：规则来源与原始／改造模板版本分别选择。Clash、Mihomo、OpenClash、Stash 的 33 个 MRS 支持双源；其他格式显示具体支持边界。
- **普通订阅转换**：内置 MihomoPro 的 Clash/Mihomo 与 Stash 完整配置支持本机／上游原始 MRS。生成时读取固定同提交资源并内嵌规则，来源参数受签名保护；不会把节点订阅改成另一地址。
- **自定义分流订阅**：统一 MetaCubeX 来源控件，保留旧内嵌方案；来源切换需主动预览并保存。
- **规则与配置源**：666OS 和 MetaCubeX 始终提供本机与上游入口，依据真实文件校验可用性和摘要。MetaCubeX 增加当前／历史修复、历史分页、磁盘用量及告警，历史修复不切换当前版本。

## 可靠性修复

- 仅 YYDS 配置或备用来源状态变化也能生成新发布版本。
- 本机清单损坏可从可信快照恢复；分支接口失败时仍可补齐已知提交原件。失败候选及时清理，不覆盖有效发布。
- 超过 16 MiB 的转换结果、超过 2 MiB 的 INI 拒绝截断交付；HTML 等无效配置保留最后有效缓存。
- 本机原生、派生、固定资源的依赖归属正确识别；同域未知路径不冒充外部或已覆盖资源。
- 修复 Stash HY2/TUIC 字段适配与冲突检测；新普通转换的规则不会被旧 Stash 美化逻辑覆盖。
- 修复 Linux virtiofs 使用块大小而非片段大小造成的磁盘统计放大。

## 验证与边界

Go 竞态与静态检查、前端状态回归、安装和 Linux 同步回归、Ruby 节点注入及隔离页面检查通过。

MihomoPro 两变体由真实内核加载 **33 个集合、171,225 条规则**。普通转换通过真实 subconverter-ng 和 Mihomo：四种目标／来源组合均为 **8 个合成节点、44 个策略组、170,536 条内嵌规则**；同一目标两来源正文一致，广告、AI、私有 IP 和兜底规则均经过核对。另外从 GitHub 固定提交联网核对 33 个 MRS，摘要全部与本机一致。当前模板引用 28 个原件，另 5 个为备用，保留原有策略顺序。

Stash 通过结构与兼容解析检查，尚未运行原生 Stash 客户端。普通转换的初始双源范围不含 88 个第三方 INI 的全部嵌套依赖，也不含其他客户端的等价适配。客户端 DNS、图标和原始节点订阅分别追溯，选择本机分流不代表整个客户端不访问网络。

旧链接缺省来源字段时保留原签名和规则链路；旧文件和方案不批量改写。固定历史资源不会自动删除，请保留升级前的数据备份。安装新镜像、同步新规则和切换已有用户配置是分别进行的操作。

Docker 工作流未配置仓库发布凭据时仅验证双架构构建；已认证维护者可单独推送并核对摘要。镜像／Release 发布与生产服务升级分别记录。
