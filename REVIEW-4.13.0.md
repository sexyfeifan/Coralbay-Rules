# 4.13.0 验证记录

日期：2026-09-08。使用独立本地数据目录与测试节点，不改写生产订阅。发布和线上升级状态应分别核实，不能由本记录推断。

## 自动验证

- 全项目 `go test -race ./...` 与 `go vet ./...` 通过；源同步总量限制新增后，源模块竞态测试另行通过，最终全项目普通测试通过。
- `bash -n install.sh`、`sh -n sync.sh`、安装器回归通过；同步回归在无网络 Linux 容器中通过。
- 三个前端脚本语法、已有导航回归、旧方案保留内嵌／新方案默认本机的 Node 状态检查通过。
- 文档相对文件链接和 13 段 Shell 示例语法通过。
- Docker 镜像构建与隔离启动通过，固定资源在应用重启后仍可读取。

## 规则原件与来源

- 真实 666OS 原始目录完成 33 / 33；MetaCubeX 完成 78 / 78。
- 111 个固定规则 URL 实际 HTTP 下载，逐文件 SHA-256 与资源目录记录一致。
- 四类 PPanel MRS 模板分别下载本机与上游变体，核对规则 URL 全部替换为相应固定版本地址，原模板未被写回。
- 本地详情在规则上游断开时仍成功；上游检查与详情不更改本地活动版本。缺失、损坏、未发布文件、旧快照补齐、同步失败和并发读取均有回归。
- 单文件限制之外，候选原始文件总量限制为 64 MiB；使用含大量注释的小规则测试，超限时拒绝候选并保留有效版本。
- 普通 INI 检查区分外部规则、嵌套未知依赖和本机零条目占位；外部 URL 查询参数中的本机 URL 不会误算成本机覆盖。

## 实际内核验证

使用镜像固定的 Mihomo 内核，在 `--network none` 的 Linux 容器中运行：

- 真实 MetaCubeX 提交 `c9f1fd664797ccdc85783c5746827a3f22ce6068` 的全部 78 个集合实际加载 **161,732 条原始 payload**，逐文件数量一致。此数量包含原始重复项，与规范化去重后的显示总数可能不同。
- 本机／上游各 5 个合成集合，通过独立 HTTP 地址下载到空缓存，文件摘要与原件相等。
- 实际匹配域名、关键词、带 `{0,61}` 量词逗号的正则、IPv4/IPv6、直连、细分规则顺序与最终兜底。测试目的地址使用拒绝策略，未建立真实代理连接。
- 生产校验代码的私有 Unix Socket 查询确实执行，核对集合加载数量。负向测试中，`-t` 放行但内核丢弃的坏正则被数量不一致检查阻止发布。

Stash 使用官方集合格式，已验证结构、规则引用和来源选择，尚无原生客户端加载与命中结果。

## 页面验证

在本地隔离预览验证登录、两个规则资源页、真实 OpenAI 本地与上游详情和摘要比较、新方案来源默认值。最终页面脚本使用应用版本作为地址参数，避免升级后沿用旧脚本。PPanel 来源地址与普通预设依赖信息同时核对。

当前未交付历史固定版本选择、资源包导入导出或第三方 INI 全量递归镜像；界面不会将这些标为已完成。
